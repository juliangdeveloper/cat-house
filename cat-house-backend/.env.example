# ===================================================================
# Cat House Backend - Environment Variables Example
# ===================================================================
# 
# SECURITY NOTE: 
# - This file contains PLACEHOLDER values only
# - NEVER commit real credentials to version control
# - For local development: Copy to .env and use your dev database
# - For production: Set secrets via GitHub Secrets (see CONFIGURATION-GUIDE.md)
#
# Required GitHub Secrets for production deployment:
# - DATABASE_URL: Neon PostgreSQL connection string
# - MIGRATION_DATABASE_URL: Neon PostgreSQL direct connection (for migrations)
# - JWT_SECRET: Secret key for JWT tokens (min 32 characters)
# - ENCRYPTION_KEY: Encryption key (exactly 32 characters)
# ===================================================================

# Neon PostgreSQL - Shared Database Configuration
# All services share the SAME DATABASE_URL (pooler endpoint for runtime)

# Runtime connection (async, pooler) - Use for all services
# Format: postgresql+asyncpg://user:password@host/database
DATABASE_URL=postgresql+asyncpg://user:password@host.region.aws.neon.tech/database

# Migrations connection (sync, direct) - ONLY for auth-service
# Format: postgresql://user:password@host/database?sslmode=require
MIGRATION_DATABASE_URL=postgresql://user:password@host.region.aws.neon.tech/database?sslmode=require

# Neon Project Configuration (optional, for reference)
NEON_PROJECT_ID=your-project-id
NEON_BRANCH_ID=your-branch-id

# Service-specific settings
# Generate secure JWT secret: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-required

# Generate secure encryption key: openssl rand -hex 32
ENCRYPTION_KEY=your-encryption-key-exactly-32ch

DEBUG=True
ENVIRONMENT=development

# CORS Configuration
ALLOWED_ORIGINS=["http://localhost:3000","https://app.gamificator.click"]

# Connection Pooling (Neon Free Tier: 10 connections max)
# Auth service: pool_size=2, max_overflow=1 (max 3)
# Catalog service: pool_size=2, max_overflow=1 (max 3)
# Installation service: pool_size=2, max_overflow=1 (max 3)
# Proxy service: pool_size=1, max_overflow=0 (max 1)
# Total: 10 connections
